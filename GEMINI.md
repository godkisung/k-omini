제공해주신 내용을 바탕으로 작성하신 **K-Omnidoc Benchmark 데이터셋** 설명 문구를 Markdown 양식에 맞춰 깔끔하게 정리해 드립니다. 특히 문제 해결 과정에서 분석하신 **"첫 페이지 튕김 현상"**에 대한 기술적 분석 내용을 적절한 위치에 배치하여, 향후 유지보수 시 참고할 수 있도록 구성했습니다.

---

# 📄 K-Omnidoc Benchmark 데이터셋 가이드

이 디렉토리는 문서 이해(Document AI) 모델의 학습 및 평가를 위한 **K-Omnidoc Benchmark** 데이터셋과 관련 도구들을 포함하고 있습니다.

## 📂 디렉토리 구조 (Directory Overview)

* **`data/`**: 벤치마크의 핵심 데이터가 저장된 곳입니다. 각 파일은 문서의 한 페이지에 해당하며, 레이아웃 정보가 담긴 `.json` 파일과 원본 페이지 이미지인 `.jpg` 파일로 구성됩니다.
* **`scripts/`**: 데이터셋을 분석하고, 시각화하며, 처리하는 데 사용되는 Python 스크립트 모음입니다. 데이터 구조를 이해하고 활용하는 예제로 활용 가능합니다.
* **`visualizations/`**: 데이터셋의 시각화 결과가 저장되는 위치입니다.
* **`[SKT-Alchera] 문서이해 작업지시서.pdf`**: 데이터셋의 공식 가이드 문서입니다. 데이터 구조, 어노테이션 규칙, 카테고리 정의 등 상세 명세가 포함되어 있습니다.

---

## 🔑 주요 파일 상세 (Key Files)

### 1. 가이드라인

* **`[SKT-Alchera] ... 작업지시서.pdf`**: 프로젝트의 목적, 데이터 구조, 어노테이션 규칙을 이해하기 위한 가장 중요한 문서입니다. 작업을 시작하기 전에 반드시 숙지해야 합니다.

### 2. 어노테이션 데이터 (`data/*.json`)

각 문서 페이지에 대한 구조화된 데이터이며 주요 키는 다음과 같습니다.

* **`layout_dets`**: 페이지 내에서 감지된 각 객체(텍스트 블록, 표, 그림 등)의 배열입니다. `category_type`(종류), `poly`(좌표), `text`(텍스트 내용) 및 속성 정보를 포함합니다.
* **`page_info`**: 페이지 메타데이터(이미지 경로, 크기, 언어, 레이아웃 유형 등)를 담고 있습니다.
* **`extra`**: 객체 간의 논리적 관계(`parent_son`, `truncated` 등)를 정의합니다.

### 3. 유틸리티 스크립트 (`scripts/*.py`)

* `visualize_annotations.py`: JSON 어노테이션을 이미지 위에 시각화합니다.
* `analyze_*.py`: 특정 기준에 따라 데이터셋의 통계를 분석합니다.
* `split_json.py`: 데이터 처리를 위해 대용량 JSON 파일을 분할합니다.

---

## 💡 개발 참고 사항 (Troubleshooting)

### Streamlit 검수 도구: "첫 페이지 튕김" 이슈 분석

검수 도구 사용 시 버튼 클릭 후 첫 페이지로 리다이렉트되는 현상에 대한 기술적 원인입니다.

1. **버튼 클릭**: 사용자가 "다음 어노테이션" 또는 "파일 이동" 버튼을 클릭합니다.
2. **전체 재실행**: Streamlit의 특성상 스크립트가 최상단부터 다시 실행됩니다.
3. **검색어 유실**: 재실행 순간 사이드바의 `search_query`가 빈 값으로 인식되는 타이밍 이슈가 발생할 수 있습니다.
4. **목록 변경**: 검색어가 사라지면서 `filtered_files`가 전체 목록으로 바뀌게 됩니다.
5. **인덱스 꼬임**: 현재의 `file_index`가 새로 바뀐 목록 범위를 벗어나거나 매칭되지 않아 로직에 의해 0번(첫 파일)으로 강제 초기화됩니다.
* *해결책*: 검색어(`last_search_query`)와 인덱스를 `st.session_state`에 고정하여 재실행 시에도 필터 목록이 유지되도록 설계해야 합니다.



---

## 🚀 활용 방법 (Usage)

`data/`에 포함된 JSON 파일을 사용하여 문서 이해 모델을 개발하고 평가합니다. 모델 개발 전 `scripts/` 디렉토리의 예제들을 참고하여 데이터를 로드하고 전처리하는 과정을 파악하는 것을 권장합니다.

---

이 Markdown 내용을 프로젝트의 `README.md`나 관련 문서 파일에 붙여넣어 사용하시면 됩니다. 추가로 수정이 필요한 부분이 있으면 말씀해 주세요! 가령, **데이터셋의 통계 정보(클래스 개수 등)**를 표 형태로 추가해 드릴까요?